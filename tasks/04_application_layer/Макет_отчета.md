<p align="center">Министерство образования Республики Беларусь</p>
<p align="center">Учреждение образования</p>
<p align="center">"Брестский Государственный технический университет"</p>
<p align="center">Кафедра ИИТ</p>
<br><br><br><br><br><br>
<p align="center"><strong>Лабораторная работа №4</strong></p>
<p align="center"><strong>По дисциплине:</strong> "Проектирование интернет-систем"</p>
<p align="center"><strong>Тема:</strong> "Application Layer: Commands, Queries, Handlers"</p>
<br><br><br><br><br><br>
<p align="right"><strong>Выполнил:</strong></p>
<p align="right">Студент 3 курса</p>
<p align="right">Группа ______</p>
<p align="right">_[ваше ФИО]_</p>
<p align="right"><strong>Проверил:</strong></p>
<p align="right">Несюк А.Н.</p>
<br><br><br><br><br>
<p align="center"><strong>Брест 2026</strong></p>

---

## Цель работы

Реализовать **приклад� ой слой** (Application Layer) с разделением операций на **команды** (изменяют состояние) и **запросы** (читают данные) по паттерну CQRS.

---

## Вариант №___ - _[Название системы]_

**Питч:** _[Из ВАРИАНТЫ_ЛАБОРАТОРНЫХ.md]_  
**Ядро домена:** _[Основные сущности]_

---

## Ход выполнения работы

### 1. Команды (Commands)

**Созданные команды:**

1. **_[CreateXCommand]_** - _[описание]_
   - Поля: `_[список полей]_`
   - Валидация: _[какая валидация примитивов]_
   - Файл: `application/command/_[файл]_.py`

2. **_[UpdateYCommand]_** - _[описание]_
   - Поля: `_[поля]_`
   - Файл: `application/command/_[файл]_.py`

**Пример кода команды:**
```python
_[Вставьте код одной команды]_
```

---

### 2. Command Handlers

**Созданные обработчики:**

1. **_[CreateXHandler]_** - _[что делает]_
   - Шаги обработки: _[валидация, создание, сохранение, события]_
   - Возвращает: _[ID или void]_
   - Файл: `application/command/handlers/_[файл]_.py`

2. **_[UpdateYHandler]_** - _[что делает]_
   - Шаги: _[загрузка, изменение, сохранение]_
   - Файл: `application/command/handlers/_[файл]_.py`

**Пример кода handler:**
```python
_[Вставьте код обработчика с комментариями]_
```

**Скриншот теста:**

_[Pytest для проверки handler]_

---

### 3. Queries (Запросы)

**Созданные запросы:**

1. **_[GetXByIdQuery]_** - _[описание]_
   - Поля: `_[ID поле]_`
   - Файл: `application/query/_[файл]_.py`

2. **_[ListActiveXQuery]_** - _[описание]_
   - Поля: `_[фильтры, пагинация]_`
   - Файл: `application/query/_[файл]_.py`

**Read DTOs:**

- **_[XDto]_** - упрощённая модель для чтения
   - Поля: `_[список полей без методов]_`
   - Файл: `application/query/dto/_[файл]_.py`

**Пример кода:**
```python
_[Вставьте код Query и DTO]_
```

---

### 4. Query Handlers

**Созданные обработчики запросов:**

1. **_[GetXByIdHandler]_** - _[что делает]_
   - Репозиторий: `_[какой repository использует]_`
   - Возвращает: `_[XDto]_`
   - Файл: `application/query/handlers/_[файл]_.py`

**Пример кода:**
```python
_[Вставьте код Query Handler]_
```

**Скриншот:**

_[Результат выполнения запроса]_

---

### 5. Application Service (Фасад)

**Реализованный сервис:** `_[RequestService / OrderService]_`

**Методы:**

| Метод | Тип | Возвращает |
|-------|-----|------------|
| `create_X(command)` | Command | ID |
| `update_Y(command)` | Command | void |
| `get_X_by_id(query)` | Query | XDto |

**Пример кода:**
```python
_[Вставьте реализацию сервиса-фасада]_
```

---

### 6. Тестирование

**Юнит-тесты:**

| Тест | Что проверяет | Статус |
|------|---------------|--------|
| `test_create_handler` | Создание агрегата, вызов save() | ✅ |
| `test_handler_publishes_events` | Публикация доменных событий | ✅ |
| `test_query_handler_not_found` | Обработка ошибки "не найдено" | ✅ |

**Скриншот pytest:**

_[Вывод pytest с 100% покрытием handlers]_

---

## Таблица критериев оценки

| Критерий | Баллы | Выполнено |
|----------|-------|-----------|
| Команды (DTOs): иммутабельность, валидация примитивов | 15 | ❌ / ✅ |
| Command Handlers: транзакции, события, сохранение | 25 | ❌ / ✅ |
| Запросы (DTOs): read-модели без побочных эффектов | 10 | ❌ / ✅ |
| Query Handlers: преобразование домена в DTO | 15 | ❌ / ✅ |
| Application Service (фасад): делегирование | 20 | ❌ / ✅ |
| Юнит-тесты handlers: mocker, события | 10 | ❌ / ✅ |
| Качество документации | 5 | ❌ / ✅ |
| **ИТОГО** | **100** | |

---

## Бонусы

| Бонус | Баллы | Выполнено |
|-------|-------|-----------|
| REST API контроллер (HTTP endpoints) | +5 | ❌ / ✅ |
| Bean Validation (@NotBlank, @Valid) | +4 | ❌ / ✅ |
| Exception Handling (глобальный обработчик) | +3 | ❌ / ✅ |
| OpenAPI документация (Swagger) | +3 | ❌ / ✅ |

**ИТОГО бонусов:** _[число]_ / 15

---

## Контрольные вопросы

1. **В чём разница между Command и Query?**
   - _[Ваш ответ]_

2. **Почему Command Handler возвращает только ID, а не весь объект?**
   - _[Ваш ответ]_

3. **Где должна выполняться валидация: в команде, обработчике или доменной модели?**
   - _[Ваш ответ]_

4. **Можно ли вызывать Query из Command Handler?**
   - _[Ваш ответ]_

5. **Зачем разделять Request DTO (от клиента) и Command (внутренний)?**
   - _[Ваш ответ]_

---

## Ссылка на репозиторий

👉 **GitHub:** _[URL]_

**Структура папки:**
```
lab-04/
├── Отчет.md
├── application/
│   ├── command/
│   │   ├── create_x_command.py
│   │   └── handlers/
│   │       └── create_x_handler.py
│   ├── query/
│   │   ├── get_x_by_id_query.py
│   │   ├── dto/
│   │   │   └── x_dto.py
│   │   └── handlers/
│   │       └── get_x_by_id_handler.py
│   └── service/
│       └── x_service.py
└── tests/
    ├── test_create_handler.py
    └── test_queries.py
```

---

## Вывод

✍️ _[Краткий вывод: что реализовано, как разделены команды и запросы, какие проблемы возникли]_

**Примеры:**
- "Реализовано 5 Command Handlers и 3 Query Handlers. Все handlers покрыты юнит-тестами. Команды иммутабельны, запросы не изменяют состояние."
- "Фасад Application Service делегирует вызовы специализированным handlers. Доменные события публикуются после сохранения."

---

**Дата выполнения:** _[Дата]_  
**Оценка:** _____________  
**Подпись преподавателя:** _____________
